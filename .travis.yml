language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - cd ts2
  - npm install
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $repo_token
  local_dir: ts2/build
  on:
    branch:
      - master
      - hw7_2
      - devel
